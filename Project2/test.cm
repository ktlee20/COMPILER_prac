/* A program to perform Euclid's
   Algorithm to computer gcd */

int globaltest;
int globalarrtest[10];

int paramtest(void)
{
	return;
}

void paramtest(int a)
{
	return;
}

void paramtest(int a[])
{
	return;
}

int paramtest(int a, int a[])
{
	return 10;
}

int gcd(int u, int v)
{
	if(v == 0) return u;
	else return gcd(v,u-u/v*v);
	/*u-u/v*v == u mod v */
	
	return gcd(x,y);
}

void main(void)
{
	int x; int y;
	x = input(); y = input();
	output(gcd(x,y));

	while(1)
	{
		i = i + 1;
	}

	if(1)
	{
		u = u + 1;
	}
	else
	{
		u = u + 1;
	}

	a > b;
	b < c;
	d == e;
	k != e;
	t <= 1;
	p >= 2;	

	a = b;
	a = 1;
	a = b[1];
	(1 + 3 ) * 4 + 5 * 10 / (10 - 2);
	call(arga, argb);
	call();
}
